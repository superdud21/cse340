<%- include('partials/header') %>
<main>
    <h1><%= title %></h1>
    <p>Explore service opportunities by category and find projects that match your interests and skills.</p>
    <ul class="category-list">
        <% categories.forEach(category => { %>
            <li><%= category.name %></li>
        <% }); %>
    </ul>
</main>
<%- include('partials/footer') %>
